import React from "react";
import styled from "styled-components";

const StyledPomodoroForm = styled.form`
  color: red;
`;

const PomodoroForm = () => {
  return <StyledPomodoroForm>Pomodoro Form </StyledPomodoroForm>;
};

export default PomodoroForm;
